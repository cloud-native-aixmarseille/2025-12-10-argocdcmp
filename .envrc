#!/bin/env direnv

# shellcheck disable=SC1008

# kube
DIR="$(realpath ${BASH_SOURCE[0]%/*})"
# KUBECONFIG="$DIR/.kube/config"

# helm
HELM_HOME="$DIR/.helm"
HELM_CACHE_HOME="$HELM_HOME/cache"
HELM_CONFIG_HOME="$HELM_HOME/config"
HELM_DATA_HOME="$HELM_HOME/data"

# exports
# export KUBECONFIG
export HELM_HOME HELM_CACHE_HOME HELM_CONFIG_HOME HELM_DATA_HOME

# demo-magic settings
export TYPE_SPEED=50      # Accelerate typing
export DEMO_CMD_COLOR=""  # No bold
export DEMO_PROMPT="${PURPLE}$ ${COLOR_RESET}"
export DEMO_COMMENT_COLOR=$CYAN
